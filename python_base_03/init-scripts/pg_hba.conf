# TYPE  DATABASE        USER            ADDRESS                 METHOD

# Allow local connections inside the container (e.g., for healthchecks)
local   all             all                                     peer

# Allow your Flask app container by Docker hostname
host    all             all             flask_app/32            md5

# Block all other external connections
host    all             all             0.0.0.0/0               reject
